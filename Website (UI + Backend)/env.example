# SNS Database Server Configuration
# Copy this to .env.local and update with your actual database server URL
NEXT_PUBLIC_SNS_API_URL=http://localhost:3000

# Note: NEXT_PUBLIC_ prefix is required for client-side access in Next.js
